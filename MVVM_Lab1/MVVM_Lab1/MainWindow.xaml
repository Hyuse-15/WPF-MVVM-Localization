<Window x:Class="MVVM_Lab1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:MVVM_Lab1.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Localization[WindowTitle]}" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewmodels:MainWindowViewModel />
    </Window.DataContext>

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2C3E50"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#3498DB"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#ECF0F1"/>

        <Style TargetType="Border">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#34495E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>

                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с выбором языка -->
        <Border Grid.Row="0" 
                Background="{StaticResource PrimaryColor}" 
                CornerRadius="5"
                Margin="0,0,0,10"
                Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Localization[MainTitle]}" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Localization[LanguageLabel]}" 
                               Foreground="White" 
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding Languages}"
                              SelectedItem="{Binding SelectedLanguage}"
                              DisplayMemberPath="NativeName"
                              Width="120"
                              Background="White"/>
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Grid.Row="1"
                   Text="{Binding Localization[TabTitle]}"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource PrimaryColor}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <TabControl Grid.Row="2" 
                    Background="White"
                    BorderThickness="1"
                    BorderBrush="#BDC3C7">
            <TabItem Header="{Binding Localization[TabDefaultHeader]}">
                <ContentControl Content="{Binding DefaultBindingControl}" />
            </TabItem>

            <TabItem Header="{Binding Localization[TabTwoWayHeader]}">
                <ContentControl Content="{Binding TwoWayBindingControl}" />
            </TabItem>

            <TabItem Header="{Binding Localization[TabOneTimeHeader]}">
                <ContentControl Content="{Binding OneTimeBindingControl}" />
            </TabItem>

            <TabItem Header="{Binding Localization[TabOneWayHeader]}">
                <ContentControl Content="{Binding OneWayBindingControl}" />
            </TabItem>

            <TabItem Header="{Binding Localization[TabTriggersHeader]}">
                <ContentControl Content="{Binding TriggersControl}" />
            </TabItem>
        </TabControl>

        <Border Grid.Row="3" 
                Background="#E0E0E0" 
                CornerRadius="3"
                Padding="5"
                Margin="0,10,0,0">
            <TextBlock Text="{Binding Localization[StatusReady]}" 
                       FontSize="10"
                       Foreground="Gray"/>
        </Border>
    </Grid>
</Window>